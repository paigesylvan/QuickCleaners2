import React from 'react';
import styles from '../styles/layout.scss'

const Header = () => {
    return (
        <div className="footer" styles={styles.footer}>
     
        </div>
    )
};

export default Header;